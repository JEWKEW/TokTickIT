# Lab 2 — AI Use and Reflection

**LLM/agent used:** Gemini 3.6 Flash (Antigravity AI Assistant)

## Selected Key Prompts (Lab 02 Features)

| # | Prompt (Summarised) | What I Did with the Result |
|---|---|---|
| 1 | Verified specification documents (specification.md, tests.md, ui-spec.md, api-spec.md), business rules BR-01 to BR-08, Given-When-Then criteria, REST API shapes, and Zen Green design guidelines. | Working on ISSUE-01 on branch feature/1-spec-and-test-plan: Draft the complete specification and test plan files in docs/lab-02/ (specification.md, tests.md, ui-spec.md, api-spec.md) . Define the MVP scope for a Requester ticketing system: Development Requester selector (simulated login), ticket creation with file uploads, paginated My Tickets with search/filter/sort, read-only Ticket Detail, and attachment soft-removal . Include numbered BRs, Given-When-Then ACs, REST API contracts, responsive Zen Green theme tokens, and a Definition of Done . Do not write application code yet. |
| 2 | Generated Prisma database schema, ran migrations, executed idempotent seed script with 4 categories, 6 related systems, 4 active and 1 inactive requesters, and verified data integrity. | Working on ISSUE-02 on branch feature/2-db-schema-and-seed: Create the PostgreSQL Prisma schema with models: RequesterUser (id, name, email, isActive), Category (id, name, isActive), RelatedSystem (id, name, isActive), Ticket (id, ticketNumber unique, requesterId, categoryId, relatedSystemId, summary, description, requestedPriority, itPriority, currentStatus default 'New', createdAt, updatedAt), and Attachment (id, ticketId, originalFileName, storedFileName, fileSize, mimeType, isRemoved default false, removalReason, removedAt, createdAt) . Write an idempotent seed script providing 4 categories ('Account and Access', 'Hardware', 'Software', 'Network'), 6+ related systems ('Email', 'Campus Wi-Fi', 'VPN', 'LEB2 App', 'Grade Submission App', 'Corporate Laptop'), 4 active requesters, and 1 inactive requester . Run and verify migrations. |
| 3 | Implemented GET /api/requesters endpoint, frontend Requester Context Selection UI in Zen Green (#006B3C), top navbar indicator with 'Change Requester' feature, and unit/component tests. | Working on ISSUE-03 on branch feature/3-dev-requester-context: Implement GET /api/requesters returning only active requesters (isActive=true) . Create a Development Requester Selection screen styled with Zen Green (#006B3C) showing an active-user dropdown, a notice that authentication arrives in Lab 3, and a Continue button. Store the selected requester in client state/session, show the active requester name in the top navbar, and provide a 'Change Requester' button that clears context and reloads data. Add unit and component tests. |
| 4 | Implemented POST /api/tickets with unique ticket number generation (TKT-YYYY-XXXXXX), Create Ticket form with client/server validation, file upload handling, and unit/API test suites. | Working on ISSUE-04 on branch feature/4-ticket-creation: Implement POST /api/tickets creating a ticket linked to the active requester with auto-generated unique Ticket Number (e.g. TKT-YYYY-XXXXXX) and default status 'New'. Build the Create Ticket page with fields: Category (dropdown), Related System (dropdown), Requested Priority (Low/Medium/High/Urgent), Summary (required, max 100 chars), Description (required, max 1000 chars), and file attachments. Validate inline on client and server, preserve input values on API failure, show busy submit state, and display success confirmation with the new Ticket Number. Write unit and API tests. |
| 5 | Built GET /api/tickets filtered strictly by active requester ID, My Tickets dashboard UI with search/filters/sorting/pagination, loading/empty/no-results UI states, and data isolation tests. | Working on ISSUE-05 on branch feature/5-my-tickets-list: Implement GET /api/tickets supporting query parameters (search, categoryId, priority, status, sort, order, page, limit) strictly filtered by the active requester ID. Build the My Tickets UI with search bar, filter dropdowns, sortable table columns (Ticket No, Created Date, Summary, Category, Priority, Status), 'Clear Filters' button, and pagination controls. Implement distinct UI states for loading, empty list, no-results match, and API failure. Add API and UI tests verifying data isolation between requesters. |
| 6 | Implemented GET /api/tickets/:id with strict requester ownership checks (403/404), read-only Ticket Detail view with Zen Green badges, and security/component unit tests. | Working on ISSUE-06 on branch feature/6-ticket-detail-view: Implement GET /api/tickets/:id enforcing that the ticket belongs to the active requester (return 404 or 403 if owned by another requester). Build the Requester Ticket Detail screen displaying read-only ticket header info, classification, description, and status/priority badges in Zen Green styling. Include a 'Back to My Tickets' link. Do not include IT staff controls, comments, or status workflow changes. Write API security tests and UI component tests. |
| 7 | Implemented attachment upload/download/soft-removal APIs, enforced file type (JPG/PNG/WEBP/PDF) and size (5MB) constraints, removal reason prompting, metadata preservation, and lifecycle tests. | Working on ISSUE-07 on branch feature/7-attachment-lifecycle: Implement attachment upload (POST /api/tickets/:id/attachments), secure file download (GET /api/attachments/:id/download), and soft-removal (DELETE /api/attachments/:id). Enforce constraints: allowed types (JPG, JPEG, PNG, WEBP, PDF), max 5 MB per file, and max 5 active attachments per ticket. On soft removal, prompt the user for a removal reason, set isRemoved=true with timestamp, keep metadata visible in the UI, and block any subsequent preview/download attempts. Write full API and UI lifecycle tests. |
| 8 | Created Playwright E2E test suite (requester-ticket-flow.spec.ts), captured responsive screenshots (1280px, 768px, 375px) under artifacts/lab-02/screenshots/, and verified layout accessibility and Zen Green styling. | Working on ISSUE-08 on branch feature/8-e2e-and-visual-audit: Write an end-to-end Playwright test suite (requester-ticket-flow.spec.ts) testing the complete flow: select requester -> create ticket with attachment -> locate in My Tickets -> view Ticket Detail -> soft-remove attachment -> switch requester and verify ticket isolation. Capture responsive screenshots at Desktop (1280px), Tablet (768px), and Mobile (375px) saved under artifacts/lab-02/screenshots/. Verify Zen Green tokens (#006B3C, #0B7A46, #EAF6EF), accessible labels, and no layout clipping. |
| 9 | Merged feature branches into lab2-staging, verified clean test runs, populated reviewer.md and ai-use.md documentation, updated README, and created release PR to main. | Working on ISSUE-09 on branch feature/9-release-prep-lab2: Validate that all feature branches are merged into lab2-staging and create a release PR from lab2-staging to main. Verify all automated unit, API, UI, and E2E tests pass from clean commands. Populate docs/lab-02/reviewer.md with peer review logs and approvals, and docs/lab-02/ai-use.md with prompt logs and personal reflection. Ensure the README includes complete setup, migration, and test instructions. |

## Reflection

Using an AI pair programmer throughout Lab 02 accelerated contract design, database schema modeling, and full-stack component construction. Providing explicit file paths, precise business rules (e.g., BR-01 to BR-08), and pre-defined test matrices allowed the AI assistant to produce type-safe, production-ready code with minimal iteration.

Key takeaways and adjustments made during development:
1. **Security & Data Scoping**: Explicitly instructing the AI to enforce `x-user-id` header validation across all ticket and attachment endpoints ensured strict requester data isolation (preventing unauthorized cross-requester access via HTTP 403 Forbidden).
2. **Attachment Soft Removal**: Auditing file removal logic led to storing removal metadata (`isRemoved = true`, `removedAt`, `removalReason`) while preserving file records for auditability while blocking actual file downloads on soft-removed items.
3. **Responsive Visual Testing**: Automating Playwright viewports across 1280px (Desktop), 768px (Tablet), and 375px (Mobile) caught container overflow edge cases early, verifying the Zen Green color palette tokens (`--zg-primary: #006b3c`) consistently across all breakpoints.
